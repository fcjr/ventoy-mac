name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Build CLI
        run: make

      # No signing identity on CI; the app builds and stays unsigned.
      - name: Build app
        run: make app
